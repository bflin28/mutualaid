# Install torch/torchvision/torchaudio separately for your platform/CUDA, e.g.:
# Linux GPU (CUDA 12.1): pip install torch==2.4.1+cu121 -f https://download.pytorch.org/whl/cu121
# CPU / macOS: pip install torch==2.4.1 --index-url https://download.pytorch.org/whl/cpu
transformers==4.46.3
datasets==3.1.0
peft==0.13.2
accelerate==1.1.1
# Optional for 4-bit/8-bit on Linux GPU; skipped on macOS where wheels are unavailable.
bitsandbytes==0.42.0; platform_system != "Darwin"
numpy>=1.26.4
