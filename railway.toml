[build]
builder = "NIXPACKS"
buildCommand = "cd training/peft && pip install -r requirements.txt"

[deploy]
startCommand = "cd training/peft && uvicorn slack_api:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
